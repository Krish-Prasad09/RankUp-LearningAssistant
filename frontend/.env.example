# API Base URL (defaults to "/api" if not specified, which proxies/directs to the backend)
VITE_API_URL=http://localhost:8000/api

# Google OAuth Client ID for GIS sign-in
VITE_GOOGLE_CLIENT_ID=your_google_oauth_client_id_here.apps.googleusercontent.com
